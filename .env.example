# OpenClaw Office 环境变量
# 复制此文件为 .env.local 并填入实际值

# Gateway WebSocket 地址（默认 ws://localhost:18789）
VITE_GATEWAY_URL=ws://localhost:18789

# 预填登录表单的 Token（可选，认证在浏览器登录界面完成）
# VITE_GATEWAY_TOKEN=

# Mock 模式（设为 true 可不连接真实 Gateway 进行开发）
# VITE_MOCK=true
